query FilterUsers($input: FilterUsersArgs!) {
  filterUsers(input: $input) {
    data {
      id
      createdAt
      userName
      email
      authProvider
      isCompleted
      friendshipStatus 
    }
    count
  }
}